# Request Traceability Table

This table manually traces each sample request in `mock_data/requests.json` through the four tool specifications:
- `check_budget`
- `check_vendor_duplication`
- `check_policy_compliance`
- `assess_risk`

Decision precedence used for all rows: `escalate > deny > approve`.

| Request | Tools Called | check_budget Return | check_vendor_duplication Return | check_policy_compliance Return | assess_risk Return | Decision Rule Applied | Expected Decision | Suggested Rationale Text |
|---|---|---|---|---|---|---|---|---|
| REQ-001 | all four tools | `{within_budget:true, remaining_budget:187550, overage:0}` | `{conflicting_vendor_ids:[], deny_significant:false}` | `{violations:[]}` | `{vendor_id:V-002, compliance_flag:false, contract_status:active, risk_level:low}` | No escalate or deny triggers; approve path applies | approve | Budget check passed for CC-001 and no overage was detected. No POL-001 duplication conflict or policy violation was found. Vendor V-002 is low risk with an active contract, so approval is recommended. |
| REQ-002 | all four tools | `{within_budget:true, remaining_budget:210800, overage:0}` | `{conflicting_vendor_ids:[V-016], conflicts:[{vendor_id:V-016, contract_id:CTR-2024-0188, contract_status:active}], deny_significant:false}` | `{violations:[]}` | `{vendor_id:V-005, compliance_flag:false, contract_status:active, risk_level:low}` | Conflicts exist but no deny-significant POL-001 condition; approve path applies | approve | CC-004 has sufficient remaining budget for this request. Another active hardware vendor exists, but the request vendor is already contracted and no deny-significant single-source violation is triggered. No policy violations were returned and risk is low, so approval is recommended. |
| REQ-003 | all four tools | `{within_budget:true, remaining_budget:348700, overage:0}` | `{conflicting_vendor_ids:[], deny_significant:false}` | `{violations:[]}` | `{vendor_id:V-007, compliance_flag:false, contract_status:active, risk_level:low}` | No escalate or deny triggers; approve path applies | approve | Budget is sufficient in CC-006 with no overage. No duplication conflict or policy violation is present. Vendor V-007 is active and low risk, so approval is recommended. |
| REQ-004 | all four tools | `{within_budget:true, remaining_budget:83600, overage:0}` | `{conflicting_vendor_ids:[], deny_significant:false}` | `{violations:[]}` | `{vendor_id:V-013, compliance_flag:false, contract_status:active, risk_level:low}` | No escalate or deny triggers; approve path applies | approve | The request remains within CC-008 budget and produces no overage. Duplication and policy checks returned no violations. Vendor V-013 is low risk with an active contract, so approval is recommended. |
| REQ-005 | all four tools | `{within_budget:true, remaining_budget:187550, overage:0}` | `{conflicting_vendor_ids:[], deny_significant:false}` | `{violations:[]}` | `{vendor_id:V-011, compliance_flag:false, contract_status:active, risk_level:low}` | No escalate or deny triggers; approve path applies | approve | Budget check for CC-001 passes with no overage. No policy violation or deny-significant duplication conflict is present. Vendor V-011 is low risk and contracted, so approval is recommended. |
| REQ-006 | all four tools | `{within_budget:false, remaining_budget:6900, overage:4300}` | `{conflicting_vendor_ids:[], deny_significant:false}` | `{violations:[{policy_id:POL-008, forced_decision:deny}]}` | `{vendor_id:V-007, compliance_flag:false, contract_status:active, risk_level:low}` | Deny trigger from budget overage and POL-008; no higher escalate trigger | deny | CC-003 budget is exceeded by 4300, so within-budget is false. Policy compliance flags POL-008 with forced deny, and no escalate-priority condition is present. Vendor risk is low, but budget violation requires denial. |
| REQ-007 | all four tools | `{within_budget:true, remaining_budget:8550, overage:0}` | `{conflicting_vendor_ids:[], deny_significant:false}` | `{violations:[{policy_id:POL-005, forced_decision:deny}]}` | `{vendor_id:V-010, compliance_flag:false, contract_status:expired, risk_level:high}` | Deny trigger from POL-005 and high risk; no higher escalate trigger | deny | Budget check passes, but vendor V-010 has an expired contract. Policy compliance returns POL-005 forced deny and risk assessment returns high risk. With no escalation override, the request should be denied. |
| REQ-008 | all four tools | `{within_budget:true, remaining_budget:210800, overage:0}` | `{conflicting_vendor_ids:[V-001,V-003], conflicts:[{vendor_id:V-001, contract_id:CTR-2024-0041, contract_status:active},{vendor_id:V-003, contract_id:CTR-2023-0198, contract_status:active}], deny_significant:true}` | `{violations:[{policy_id:POL-001, forced_decision:deny}]}` | `{vendor_id:V-012, compliance_flag:false, contract_status:none, risk_level:medium}` | Deny trigger from POL-001 duplication conflict above threshold | deny | Budget is available, but duplication check finds active contracted office supplies vendors while requested vendor V-012 is non-contracted. Because amount is above the POL-001 threshold, this is deny-significant and policy compliance returns forced deny. Risk is medium, so deny remains the final decision. |
| REQ-009 | all four tools | `{within_budget:true, remaining_budget:5500, overage:0}` | `{conflicting_vendor_ids:[], deny_significant:false}` | `{violations:[{policy_id:POL-004, forced_decision:deny}]}` | `{vendor_id:V-017, compliance_flag:false, contract_status:none, risk_level:medium}` | Deny trigger from POL-004 prohibited category | deny | The request is within CC-005 budget and has no duplication conflict. Policy compliance identifies POL-004 for catering, which forces deny regardless of amount. With no escalate-priority trigger, denial is required. |
| REQ-010 | all four tools | `{within_budget:false, remaining_budget:37900, overage:11700}` | `{conflicting_vendor_ids:[], deny_significant:false}` | `{violations:[{policy_id:POL-003, forced_decision:escalate},{policy_id:POL-008, forced_decision:deny}]}` | `{vendor_id:V-013, compliance_flag:false, contract_status:active, risk_level:low}` | Both deny and escalate present; precedence selects escalate | escalate | CC-009 budget is exceeded by 11700, which creates a deny condition. Policy compliance also returns POL-003 escalation due to near-director-threshold amount behavior. Under precedence escalate over deny, this request should be escalated with both findings cited. |
| REQ-011 | all four tools | `{within_budget:true, remaining_budget:187550, overage:0}` | `{conflicting_vendor_ids:[], deny_significant:false}` | `{violations:[{policy_id:POL-006, forced_decision:escalate}]}` | `{vendor_id:V-006, compliance_flag:true, contract_status:active, risk_level:critical}` | Escalate trigger from POL-006 and critical risk | escalate | Budget is sufficient and duplication does not trigger denial. Vendor V-006 has an active compliance flag, and policy compliance returns POL-006 forced escalate while risk is critical. Escalation is required for Legal and Compliance review. |
| REQ-012 | all four tools | `{within_budget:true, remaining_budget:51250, overage:0}` | `{conflicting_vendor_ids:[], deny_significant:false}` | `{violations:[]}` | `{vendor_id:V-014, compliance_flag:false, contract_status:active, risk_level:low}` | No escalate or deny triggers; approve path applies | approve | CC-002 has sufficient budget and the request does not exceed remaining funds. No deny-significant duplication or policy violations are returned. Vendor V-014 is contracted and low risk, so approval is recommended. |
| REQ-013 | all four tools | `{within_budget:true, remaining_budget:18000, overage:0}` | `{conflicting_vendor_ids:[], deny_significant:false}` | `{violations:[]}` | `{vendor_id:V-015, compliance_flag:false, contract_status:none, risk_level:medium}` | No escalate or deny triggers; approve path applies | approve | The request is within CC-007 budget with no overage. Duplication and policy checks produce no violations. Vendor risk is medium due to no contract, but no deny or escalate rule is triggered, so approval is recommended. |
| REQ-014 | all four tools | `{within_budget:true, remaining_budget:348700, overage:0}` | `{conflicting_vendor_ids:[V-005], conflicts:[{vendor_id:V-005, contract_id:CTR-2025-0012, contract_status:active}], deny_significant:false}` | `{violations:[{policy_id:POL-003, forced_decision:escalate}]}` | `{vendor_id:V-016, compliance_flag:false, contract_status:active, risk_level:low}` | Escalate trigger from POL-003 near-threshold condition | escalate | Budget check passes for CC-006 and no overage exists. Policy compliance returns POL-003 escalation because the amount is near the director threshold. Even without deny conditions, escalation is required by priority. |
| REQ-015 | all four tools | `{within_budget:true, remaining_budget:5500, overage:0}` | `{conflicting_vendor_ids:[], deny_significant:false}` | `{violations:[]}` | `{vendor_id:V-004, compliance_flag:false, contract_status:none, risk_level:medium}` | No escalate or deny triggers; approve path applies | approve | The request is within budget and no duplication conflict is found. Policy compliance returns no violations, and risk is medium due to no active contract. Since no deny or escalate rule fires, the recommendation should be approve under the defined priority model. |

## Notes for Test Assertion Authors

1. Every request calls all four tools.
2. Decision precedence assertions should explicitly test mixed-signal case REQ-010 (`escalate` over `deny`).
3. Vendor duplication assertions should verify conflict-list detail fields for REQ-008 (and non-deny-significant conflict cases such as REQ-002 and REQ-014).
4. REQ-015 is mapped to `approve` by deterministic precedence, even though mock data labels it as ambiguous for prompt-tuning experiments.
